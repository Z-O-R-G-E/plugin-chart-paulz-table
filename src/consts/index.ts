export enum MetricsLayoutEnum {
  ROWS = 'ROWS',
  COLUMNS = 'COLUMNS',
}

export enum TableType {
  STRAIGHT = 'STRAIGHT',
  PIVOT = 'PIVOT',
}

export enum QueryMode {
  AGGREGATE = 'AGGREGATE',
  RAW = 'RAW',
}
